# npm v0.1.3 发布证据

## 状态

- 源码：`PASS`
- 候选制品：`PASS`
- GitHub 分发：`PASS`
- npm 分发：`PASS`
- 隔离 Harness Runtime：`NOT_VERIFIED`
- 真实 profile 更新：`NOT_APPLICABLE`；本发布合同不包含部署
- 真实 consumer / 模型调用：`NOT_VERIFIED`

公共包发布不证明 DeepSeek Harness 已安装、加载或实际调用该版本。历史 Runtime 与 consumer 证据不自动转移到 `0.1.3`。

## 发布身份

| 对象 | 精确身份 |
| --- | --- |
| npm 包 | `dsh-plugin-swift-cycle@0.1.3` |
| GitHub tag | `v0.1.3` |
| 发布 commit | `0c491852234be515083590802a4404476f701028` |
| Swift Cycle 上游 | `v1.3.0` / `f6645dc2768132e48bb936147f40a954855e5ccf` |
| 上游载荷 SHA-256 | `fccc61c3c9ce91f00e3fbd1238fdf945e7829f9ffa264a4ebc12ff34fc5c8fb0` |
| npm SHA-1 | `335623c1d66ce2fa842a04d417a627265f90af68` |
| npm integrity | `sha512-o4VtKBMYCN1/UTteNOpb2jk6esXcXzF7Tl5dz7UPGRSyyxrWO+ZFcdldtqlNDW8gqXG5/tOHn4GmYZKHjIzlxA==` |
| npm 包大小 | 30,209 bytes；解包 77,250 bytes；26 个文件 |

GitHub Release：<https://github.com/Solismuchengxue/dsh_plugin_swift_cycle/releases/tag/v0.1.3>

## 发布前验证

- npm 身份门成功；未读取或记录 Token、`.npmrc` 内容。
- 18/18 Node 测试通过；RED 阶段的 4 个失败精确对应旧 vendor/lock 身份。
- vendored Swift Cycle 通过官方 `quick_validate.py`。
- 显式上游 checkout、packaged snapshot 与解包后的 npm 制品均得到相同载荷摘要。
- 26 文件 pack 合同、npm publish dry-run、30 个 Markdown 文件、27 个相对链接、UTF-8/空白/fence 检查和 `git diff --check` 通过。

## 发布后验证

- `origin/main`、本地 `v0.1.3` 和远端 `v0.1.3` 指向同一发布 commit。
- GitHub Release 为普通公开 Release，不是 draft 或 prerelease。
- npm `latest` 为 `0.1.3`；Registry `gitHead` 指向发布 commit。
- 从 Registry 重新下载的 tarball SHA-1、integrity、26 文件清单、Blueprint 文件和载荷摘要均与发布前候选一致。

## 边界

- 本次没有修改 Swift Cycle 主仓、真实 DSH profile、隔离 Runtime 或 consumer。
- `modelInvocable: false`、`userInvocable: true` 保持不变。
- 没有增加运行时依赖、安装生命周期脚本、凭据读取或隐式调用。
- 只有在固定 `0.1.3` 身份完成对应安装、加载与显式调用后，才能分别更新 Runtime、profile 和 consumer 状态。
